{
  "mcpServers": {
    "antigravity-docs": {
      "command": "node",
      "args": [
        "${PROJECT_DIR}/.agents/mcp-servers/antigravity-docs/mcp_server.js"
      ],
      "description": "Antigravity IDE documentation knowledge base — search_docs, read_doc, list_document_names. NOTE: ${PROJECT_DIR} is replaced at runtime by os-init during project setup."
    }
  }
}
