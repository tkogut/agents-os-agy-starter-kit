# Contributing to AGENTS-OS Starter Kit

We welcome contributions from the community! To contribute:
1. Fork the repository.
2. Create a feature branch: `git checkout -b feature/new-skill`.
3. Implement your changes or skills in standard `SKILL.md` format.
4. Verify changes locally using the test suite.
5. Open a Pull Request against `master` or `main`.
